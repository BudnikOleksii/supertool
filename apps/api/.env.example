# supertool API — copy to .env for local overrides.
# Defaults (baked into env.schema.ts) match docker/docker-compose.yml, so the
# native dev loop works without a .env file.
NODE_ENV=development
PORT=3001
DATABASE_URL=postgres://supertool:supertool@localhost:5432/supertool
