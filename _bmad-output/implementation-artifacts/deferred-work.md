# Deferred Work

## Deferred from: code review of 3-2-expense-breakdown-by-category (2026-06-16)

- Breakdown CTE trusts category-tree integrity that the reconciling summary query does not. `analytics.repository.ts`'s `category_roots` recursive CTE seeds only from user-owned `parent_id IS NULL` roots and descends; any category whose ancestor chain never resolves to such a root is dropped by the `INNER JOIN category_roots`, silently removing its expense from `total`/`totalExpense`/`share`, while `getMonthlySummary` (flat sum over all expense rows) still counts it — so the on-screen FR18 reconciliation (`sum(breakdown.total)` vs summary `expense`) could diverge. Happy path is fully guarded against orphans/cycles at write time (same-user parent scoping in `loadParentOrThrow`, `onDelete: 'restrict'` on `parent_id`, `assertSameType`, `assertValidNewParent` cycle check), so this is unreachable through the application surface today — robustness hardening only. If hardened: either make the breakdown roll-up tolerate unrooted categories (attribute to a synthetic root or self) or add a reconciliation assertion. [apps/api/src/modules/analytics/analytics.repository.ts]

## Deferred from: 2-5-filter-sort-the-list (2026-06-15)

- Create/edit-transaction redirects preserve only `period` (+ approximate `page`), not the active filter/sort. Story 2.5 threaded the full `type`/`categoryId`/`sortBy`/`sortOrder` view params through the **delete** flow (`TransactionRowActions` → `use-delete-transaction` → `deleteTransaction` → `redirectAfterTransactionDelete` → `buildTransactionsRedirectQuery`), because those params are live on the list at delete time. The **create/edit** flows originate on `/transactions/new` and `/transactions/[id]/edit`, which do not carry the list's filter state, so `redirectToTransactionMonth` (shared by both) still emits a period-only query (`buildTransactionsRedirectQuery` called without `view`). After adding or editing a transaction the user lands on the unfiltered month view. Capture the originating filter/sort (e.g. via a `from` query param on the new/edit links or a returnTo token) and thread it into `redirectToTransactionMonth` if this return-to-filtered-view continuity is wanted. [apps/money-tracker/src/app/[locale]/transactions/utils/redirect-to-transaction-month.ts, build-transactions-redirect-query.ts, actions/{create,update}-transaction.ts]
- `getLastPageForPeriod`/`getTransactionPage` (delete + create/edit redirect page math) compute the target page from the **unfiltered** month total, so with filters active the computed `page` can be off (it is already an approximation for same-date clusters — see 2.4 deferral). The filter/sort params are preserved in the delete redirect URL, but the page number is not filter-aware. Compute the page against the same filtered `whereClause` if exact filtered-page landing is needed. [apps/money-tracker/src/app/[locale]/transactions/utils/redirect-after-transaction-delete.ts]
- `dateFrom > dateTo` / real-calendar cross-field validation on `FindTransactionsQueryDto` remains deferred from 2.2 (the UI always derives both bounds from one `period`, so it cannot trigger it; direct-API hardening only). Not expanded in 2.5.

## Deferred from: code review of 2-6-organize-categories (2026-06-14)

- Concurrent delete-with-reassign of two sibling parents can form an orphaned cycle — `isDescendantOf` is read at READ COMMITTED with no row/advisory lock, so two interleaved deletes reparenting each other's children pass the cycle check independently. Practically unreachable in the single-operator local-only runtime; add a Postgres advisory lock (or `SELECT ... FOR UPDATE` on the affected subtree) around the reassign-then-delete transaction if the deployment model ever gains concurrency. [apps/api/src/modules/transaction-categories/transaction-categories.service.ts]
- `fetch-category-list` collapses API errors into an empty list — `const { data } = await ...findAll(...); return data ?? []` discards `error`, so a transient API failure renders the "No categories yet" empty state and the edit page silently redirects. The 401 case is already handled upstream by the page's `fetchProfile()` redirect, and the pattern likely mirrors the existing `fetch-profile` action; surface transport/API errors distinctly if this fetch path is hardened. [apps/money-tracker/src/actions/fetch-category-list.ts]
## Deferred from: code review of 2-2-browse-transactions-by-month (2026-06-14)

- No within-month pagination UI ships (`apps/money-tracker/src/app/[locale]/transactions/page.tsx`, `TransactionListServer.tsx`): `page` is parsed, fed to the API, and keys the Suspense boundary, but no pagination control renders and `meta.total` is never read on the frontend, so rows beyond `TRANSACTIONS_PAGE_SIZE=50` are unreachable within a month. **Deferred to Story 2.5** — within-month pagination belongs with the filter/sort work; Story 2.2 scope (AC4) is month stepping only.
- No `@Max` on `page` in `PaginationQueryDto` (`apps/api/src/shared/dtos/pagination-query.dto.ts`); `limit` is capped at `MAX_PAGE_SIZE=100` but `page` only has `@Min`, so an arbitrarily large `?page=N` produces an unbounded `OFFSET` and a page beyond `total` silently returns an empty list (the frontend never reads `meta.total` to clamp). Auth-scoped and read-only so no security impact; add a `@Max` + a `meta`-aware clamp when the 2.5 pagination UI lands.
- `FindTransactionsQueryDto` (`apps/api/src/modules/transactions/dtos/find-transactions-query.dto.ts`) validates only the `^\d{4}-\d{2}-\d{2}$` shape — no cross-field check that `dateFrom <= dateTo` and no real-calendar validation, so `dateFrom=2025-03-31&dateTo=2025-03-01` or `2025-02-30` / `2025-13-01` pass validation and return an empty set instead of a 400. The UI always derives both bounds from one period so it can't trigger this; harden the direct-API contract later.
- Second-user isolation assertion in `apps/api/test/integration/transactions.integration.spec.ts` queries the operator with `HIGH_LIMIT=1000`; if the operator's month window ever exceeds 1000 rows the injected other-user id could be absent via truncation rather than scoping, false-passing the test. Harden by asserting on a count or a narrow window.
- `MonthStepper` (`apps/money-tracker/src/app/[locale]/transactions/components/month-stepper/MonthStepper.tsx`) computes next/prev from the render-time `period` prop, not the freshest URL; a rapid double-click before the RSC re-render commits can land one month off. Add a pending-navigation guard if it proves noticeable.

## Deferred from: code review of 1-8-design-system-repair-theming (2026-06-12)

- Visual evidence gate (both-theme screenshots incl. open/interactive states vs `example/track-my-life`) not satisfied for 1.8's repaired primitives — deferred to Story 1.9, which establishes the screenshot-based visual QA baseline; 1.8 accepted on mechanism + token purity only.
- Select open-panel width defect (`packages/ui/src/components/select/Select.module.scss:68,87` — `.popperViewport` pinned to trigger width vs `.content` min-width 8rem clips items/check/highlight) — deferred to Story 1.9 AC-2, which diagnoses and schedules this exact fix.
- No gate links switcher constants to message keys: `THEME_OPTION_LIST` (`packages/shell/src/components/theme-switcher/constants.ts`) and `LOCALE_CODE_LIST` resolve labels via `translate(option)` with no compile-time or CI link to `navigation.json` keys — adding an option without its message key only fails at runtime. The i18n parity gate checks en↔uk symmetry only. Add a constants↔messages check next time the parity script is touched.
- Select empty-options affordance remains deferred (originally from the 1.4 review, entry below) — Story 1.8 touched Select styling/markup but the empty-state behavior was out of its AC scope.

## Deferred from: code review of 1-4-money-tracker-shell-design-system-i18n-foundation (2026-06-12)

- NavigationLink active-state is exact-match and locale-naive (`packages/next-shared/src/i18n/navigation/NavigationLink.tsx:11`). `pathname === href` is correct for the single root tool (`/`) today but won't set `aria-current="page"` for nested routes (`/tool/sub`) or non-default-locale paths. Revisit when a second tool or sub-routes land (AC-2) — likely needs prefix-matching plus an exact-match special case for the home entry.
- `Select` primitive (`packages/ui/src/components/select/Select.tsx:42`) has no empty-options or unmatched-value handling: `optionList={[]}` yields an empty popup, and a controlled `value` not present in the options renders a blank trigger (no `placeholder` wired on the Radix `Value`). No consumer hits this yet (LocaleSwitcher always has two valid options); add an empty-state / placeholder when a real consumer needs it.
- `apps/money-tracker/src/i18n/request.ts:17` dynamic-imports `messages/${locale}.json` with no fallback. Only en/uk exist today (both present and parity-gated); adding a locale to `LOCALE_CODE_LIST` without its message file would throw a runtime module-not-found before the parity gate runs. Add a fallback-to-defaultLocale-messages guard when a third locale is introduced.
- i18n-parity script (`scripts/check-i18n-parity.mjs:34`) is brittle on non-string message shapes: an empty `{}` namespace flattens to no key (divergence can slip through) and a malformed JSON file throws a raw `SyntaxError` instead of the readable per-file report. Works correctly for the current string-leaf messages and CI still fails on bad JSON; harden the flattener + wrap `JSON.parse` next time the gate is touched.

## Deferred from: code review of 1-2-api-foundation-health-check-database-baseline (2026-06-10)

- Health endpoint returns HTTP 200 with `database: 'down'` (deliberate AC-literal reading, no @nestjs/terminus). There is no machine-readable unhealthy signal at the HTTP layer — container healthchecks and probes key on status codes. Revisit when Story 1.7 wires docker compose healthchecks for the api service.
- `db:migrate` runs drizzle-kit via its internal `./node_modules/drizzle-kit/bin.cjs` path (needed for Node `--env-file-if-exists` loading). Stable under the exact 0.31.10 pin and pnpm's direct-dependency layout, but re-evaluate whenever drizzle-kit is upgraded or env handling changes.
- Error envelope (`ErrorResponseDto`) is exposed in OpenAPI via `extraModels` only; no endpoint declares per-operation `@ApiResponse` error types, so the generated client (Story 1.3) won't type error responses per endpoint. Add decorations when real failure modes land (Story 1.5+).

## Deferred from: code review of 1-1-monorepo-scaffold-quality-gates (2026-06-10)

- ~~Type-aware oxlint rules~~ — RESOLVED in Story 1.2 (2026-06-10): the two inert rules (`typescript/no-floating-promises`, `typescript/no-misused-promises`) were dropped from `packages/lint-config/configs/base.json`; enabling `--type-aware` would require the experimental `oxlint-tsgolint` toolchain (unapproved dependency). Revisit if/when oxlint stabilizes type-aware mode.
- Vendored `ui-ux-pro-max` skill (`.claude/skills/` and `.agents/skills/` mirrors) instructs agents to run `sudo apt update && sudo apt install python3` if Python is missing. Vendored third-party content; neuter or fence as human-only guidance next time the skill is touched/updated.

## Deferred from: code review of 1-9-design-system-structure-visual-qa-baseline (2026-06-13)

- Select open-panel long-label clipping (`packages/ui/src/components/atoms/select/Select.module.scss`). The AC-2 width fix is correct, but pre-existing `.content { overflow: hidden }` has no `max-width` clamp / no `--radix-select-content-available-width` and `.item` has no `white-space`/truncation — an option longer than the popper's available width clips with no ellipsis/wrap. Not introduced by this change; no current consumer hits it (short labels only). Add truncation or available-width handling when a long-label Select consumer lands.
- Storybook a11y addon (`a11y: { test: 'error' }` in `.storybook/preview.ts`) is not machine-enforced by the gate pipeline — `turbo run test` is Vitest-only, so axe coverage rests on a manual playwright+axe harness. The Select `Open` story scopes off `aria-hidden-focus` (documented, reference-matching Radix behavior); the Dialog `Open` story uses the same portal+`aria-hidden` mechanism with no suppression. Confirm Dialog `Open` is genuinely addon-clean and/or wire a real Storybook a11y gate (test-runner / `@storybook/addon-vitest`) so the visual-QA a11y claim is enforced, not asserted in the record.

## Deferred from: code review of 1-10-design-system-atom-parity (2026-06-13)

- ~~TimePicker renders an invalid/out-of-range `value` prop unclamped~~ — RESOLVED in PR review (CodeRabbit): `parseTimeString` now falls back to `DEFAULT_VALUE` on `NaN` and clamps hours/minutes to their valid ranges before display. (A bare no-colon string like `"930"` still parses its first segment, but it is now range-clamped.)
- AspectRatio `ratio={0}` (or negative) produces broken geometry — Radix computes `paddingBottom: 100/ratio %`, so `0`→Infinity, negatives→negative padding. No guard; inherited Radix/reference behaviour. Add a `ratio > 0` guard/clamp if a consumer derives ratio from possibly-zero intrinsic dimensions.
- Deeper unit-test coverage for the new atoms beyond the smoke-test bar: TimePicker keyboard-wrap (`ArrowUp`/`ArrowDown`)/disabled/NaN branches, Checkbox indeterminate + disabled + error-combination states, RadioGroup no-selection and disabled-group states, Alert no-icon layout, Avatar real (404) image→fallback failover. AC3 (smoke test) + AC4 (stateful screenshots) already cover these renders; this is branch-coverage hardening, not a correctness gap.
- Skeleton `pulse` animation has no `@media (prefers-reduced-motion: reduce)` opt-out (`packages/ui/src/components/atoms/skeleton/Skeleton.module.scss`) — a perpetual animation with no reduced-motion escape hatch. A11y enhancement at reference parity; add the reduced-motion block.
- Re-capture the Alert visual-QA screenshots (`visual-qa/1-10/alert-*.png`). The code-review patch that gated the Alert icon slot (`Alert.module.scss` `:not(.title):not(.description)`) changed the icon-less layout from title-beside-description to the intended title-stacked-above-description; the existing PNGs predate the fix. Mechanical re-capture once the throwaway Storybook screenshot harness is next stood up (no harness is committed to the repo).

## Deferred from: code review of 1-11-design-system-molecule-parity (2026-06-13)

- Combobox carries `role="combobox"` on both the trigger button and the inner search input — two combobox roles in the a11y tree when open. Mirrors the reference and the open-state axe pass is green; revisit if a consumer needs the cleaner single-combobox composition (button = `aria-haspopup="listbox"`, input = the combobox).
- `BreadcrumbEllipsis` has no `sr-only` "More" label — the ellipsis icon is `aria-hidden`, so collapsed crumbs convey nothing to assistive tech. The shadcn reference includes a visually-hidden label; add one once a `sr-only`/visually-hidden convention is settled in `packages/ui`.
- `Card` `withAction` grid layout does not engage when `<CardAction>` is wrapped in a React Fragment — `Children.toArray(...).some(child => child.type === CardAction)` does not descend into a Fragment's children. Direct-child usage (the documented shape) works; harden only if a consumer composes header children behind a Fragment.
- Combobox controlled `value` not present in `optionList` renders the placeholder (and `aria-label` falls back to the placeholder) instead of signalling a set-but-unresolved selection — reachable with async-loaded options or a stale value. Reference-parity behaviour; add a "value set but unresolved" affordance if a consumer needs it.
- `FieldError` multi-error dedup keys by `error.message`, so multiple message-less (`undefined`) entries collapse to a single Map key and render a one-item `<ul>` where the single-message plain-text fast-path was intended. Rare react-hook-form shape; markup-only, not a crash.
- Combobox trigger does not open-and-highlight on `ArrowDown`/`Enter`/`Space` — the keyboard handler is wired only to the search input inside the popover, so a keyboard user cannot open + highlight in one motion. Reference-parity; add trigger key handling if the composite-widget keyboard contract is tightened.

## Deferred from: code review of 1-5-sign-up-sign-in (2026-06-14)

- Double-submit window in the auth forms: the submit button only disables once `isPending` flips inside `startTransition`, which runs after async zod validation resolves — a rapid double-click can fire two auth requests. Sign-in is idempotent; a sign-up double-submit could surface a spurious `userExists`. Add a click/submit guard if it proves bothersome. [packages/widgets/src/components/sign-up-form/SignUpForm.tsx, sign-in-form/SignInForm.tsx]
- `AUTH_RATE_LIMIT_DISABLED` env flag has no production guard — it was introduced to let the Testcontainers integration suite exceed 5 sign-ins/60s, but a stray `=true` in any environment silently disables auth brute-force protection with no warning. Low risk for the current local-only, single-instance, single-user runtime; add a `NODE_ENV==='production'` guard (or move the override to test-only config) if the deployment model ever changes. [apps/api/src/auth/auth.ts:60, apps/api/src/app/env.schema.ts]

## Deferred from: code review of 1-6-profile-settings (2026-06-14)

- Shell locale switch couples to `userName` and always sends `name` — `AppShellSection.handleLocaleChange` returns early when `userName` is undefined (skipping persistence while `LocaleSwitcher` still navigates), and reuses `updateProfile({ name, locale })`, gating a locale-only change on name validity. `LocaleSwitcher` also awaits the persist before `router.replace`, so a failed request silently blocks the visible locale switch (and leaves an unhandled rejection). Unreachable in practice (FR5 guarantees a name); the persist-then-navigate ordering is intentional to avoid the apply-on-sign-in redirect race. Decouple from `userName` (guard on auth/profile presence) and surface persist failures if this surface is revisited. [apps/money-tracker/src/app/[locale]/AppShellSection.tsx, packages/shell/src/components/locale-switcher/LocaleSwitcher.tsx]
- New users always get `locale = 'en'` regardless of the locale they signed up under — better-auth's sign-up INSERT omits the new column and relies on the Postgres `DEFAULT 'en'`. With the apply-on-sign-in redesign, a new user now simply stays in the locale they signed up under (no forced redirect); their saved `locale='en'` only takes effect on a later explicit sign-in. Capturing the sign-up-context locale at account creation (so the profile reflects it) is out of scope for 1.6 (sign-up is story 1.5) and is a product decision, not a defect. [apps/api/src/auth/auth.ts, apps/api/src/database/schemas/users.ts]

## Deferred from: code review of 1-7-one-command-local-runtime (2026-06-14)

- Migrate-on-boot (`runMigrations`) takes no advisory lock; with `restart: unless-stopped` a boot that succeeds at migrate but fails before/at `app.listen` re-runs `migrate()` on restart, and concurrent boots (future horizontal scaling) could race. The integration test covers clean-apply + idempotent re-apply but not partial-migration-failure-then-retry. Low risk for the single-replica local runtime; add a Postgres advisory lock around migrate if the deployment model ever scales out. [apps/api/src/database/run-migrations.ts, docker/docker-compose.yml]
- The migrate-on-boot integration test resolves migrations from `src/database/migrations` (via `process.cwd()`), not the `dist/database/migrations` path the container actually boots from (`__dirname`, populated by the `nest-cli.json` `assets` glob). A break in the assets glob would pass every test and fail only at container boot — exactly the Task 2 high-risk failure mode. Add an assertion that the built `dist` contains the migration SQL + `meta/_journal.json`. Manual e2e covered it once. [apps/api/test/integration/migrate-on-boot.integration.spec.ts, apps/api/nest-cli.json]
- `runSeed()` is parameterless while `runMigrations` takes `{ databaseUrl, migrationsFolder }`. A real Epic 2 seed needs DB access, so it will require adding a parameter to `runSeed` and threading it through `prepareDatabase` + `main.ts` — minor boot-sequence surgery, mildly contradicting AC2's "fill the body only, no boot surgery later." Thread the DB handle when Epic 2 (FR17) lands. [apps/api/src/database/run-seed.ts, apps/api/src/database/prepare-database.ts]
- `outputFileTracingRoot` (monorepo root, set in `next.config.ts`) and the `web.Dockerfile` hardcoded `CMD ["node", "apps/money-tracker/server.js"]` + standalone COPY layout are coupled across two files with no shared constant or build-time assertion; changing either silently breaks the container `CMD` ("Cannot find module"). Add a guard or shared note if the tracing root ever changes. [apps/money-tracker/next.config.ts, docker/web.Dockerfile]

## Deferred from: code review of 2-1-seed-the-real-data (2026-06-14)

- Nullable `import_key` + plain `uniqueIndex` (`schemas/transactions.ts:730`) lets future NULL-key rows bypass `ON CONFLICT DO NOTHING` dedup. Seed always populates the key; this matters for the 2.2+ manual-entry insert path. Consider a partial unique index `WHERE import_key IS NOT NULL` (drizzle 0.45.2 cannot apply `nullsNotDistinct` to `uniqueIndex`).
- Category type is captured from the first-seen record and children inherit the parent's type (`seeds/derive-category-hierarchy.ts:956-974`). A category that legitimately appears under both income and expense would be silently single-typed, and a child whose own transactions differ in type from its parent would get a type-mismatched category row. Current dataset verified single-type per category; add a runtime invariant if real data ever violates it.
- `normalizeTransactionType` (`seeds/normalize-transaction-type.ts`) maps every non-"Income" string to `expense`, silently absorbing unexpected source types (e.g. "Transfer", lowercase, whitespace). Dataset is Income/Expense only; consider throwing on an unknown type to honor the "never guess / surface for review" philosophy.
- `convertAmountToString` (`seeds/convert-amount.ts`) has no NaN/negative/zero guard; a zero or negative amount would abort the entire seed via the `CHECK (amount > 0)` constraint instead of being surfaced per-row. Dataset is clean positive amounts.
- Operator creation is not atomic across `auth.api.signUpEmail` and role promotion (`seeds/seed-operator.ts`); two concurrent boots could race and throw on duplicate signup, aborting boot. Local runtime is single-container and the existing-operator path self-heals re-promotion, so not reachable today.
- Category id maps are keyed by name only (`seeds/seed-transactions.ts:59`); two parents owning a same-named child would mis-resolve transaction linkage. All 34 subcategories are verified single-parent in the current data.
- Report counters (`topLevelCreated`/`childrenCreated`/`skippedDuplicates`, `seeds/seed-transactions.ts:197`) can diverge from reality on an interrupted partial re-run (e.g. top-levels committed, children not). Cosmetic — the data ends up correct; the counters just misattribute.

## Deferred from: code review of 2-3-fast-transaction-entry (2026-06-15)

- **Amount/date validation regexes duplicated across API and frontend** — `POSITIVE_AMOUNT_PATTERN` and `CALENDAR_DATE_PATTERN` are byte-identical in `apps/api/src/shared/constants/transaction-validation.ts` and `apps/money-tracker/src/app/[locale]/transactions/constants/transaction-form-schema.ts`. A future server-side tightening silently diverges the client (client-passes/server-rejects → generic API error UX). Candidate for a single source in `@supertool/shared`. Pre-existing cross-package convention (2.2 precedent), not introduced by this story.
- **TOCTOU race on transaction create** — `transactions.service` does a non-atomic ownership pre-check (`findCategoryForUser`) then `repository.create`; a category deleted in the window FK-violates → raw 500 instead of the intended clean 404. Low-risk under the single-user local runtime with `onDelete: 'restrict'` and a very narrow window; a wrapping transaction would close it.

## Deferred from: code review of 2-4-edit-delete-transactions (2026-06-15)

- **fetch-transaction swallows non-404 errors into a phantom "not found" redirect** — `fetch-transaction.ts` destructures only `{ data }` and returns `data ?? null`; the edit page redirects any null to `/transactions`. A 401 (expired session) or 500 is therefore indistinguishable from a genuinely missing transaction and silently bounces the user to the list instead of sign-in/an error. Matches the established `fetch-*` read-action template (codebase-wide), and `fetchProfile` auth-guards earlier so the window is narrow; address as a cross-package read-action convention if desired. [apps/money-tracker/src/actions/fetch-transaction.ts]
- **updateScoped non-atomic UPDATE-then-SELECT (TOCTOU)** — `updateScoped` runs `UPDATE ... RETURNING { id }` then a separate `findOneByUserIdAndId` reload outside a transaction; a concurrent DELETE in the window makes the reload return null and the service throws a misleading 404 after a successful write. Same class as the already-tracked create TOCTOU and explicitly acknowledged in the story Dev Notes as acceptable under the single-operator local runtime (`onDelete: restrict`, narrow window). A wrapping transaction would close it. [apps/api/src/modules/transactions/transactions.repository.ts]
- **Edit-mode page-jump redirect off-by-one on same-date clusters** — `getTransactionPage` counts rows with `date > editedDate` (`getNextCalendarDate`) then `floor(count / pageSize) + 1`; list ordering is `desc(date), desc(id)`, so for an edited row that shares its date with others the page math ignores the same-date `id` tiebreaker and can land the user on a page that does not contain the just-edited row. Inherited verbatim from the create flow's known approximation; reused for edit. [apps/money-tracker/src/app/[locale]/transactions/utils/redirect-to-transaction-month.ts]

## Deferred from: code review of story-3.1 (2026-06-15)

- Analytics summary date DTO (`find-summary-query.dto.ts`) validates `dateFrom`/`dateTo` with the shape-only `CALENDAR_DATE_PATTERN` (`/^\d{4}-\d{2}-\d{2}$/`) and has no `dateFrom <= dateTo` ordering guard. Impossible-but-shaped dates (e.g. `2025-02-30`, `2025-13-01`) pass validation and could surface as a Postgres date-cast 500; a reversed range silently returns an empty summary. Pre-existing repo-wide convention — `transactions/dtos/find-transactions-query.dto.ts` uses the identical pattern with no guard. If hardened, do it repo-wide (a real calendar-date validator + range-ordering check shared across all date-range DTOs), not just in analytics.
- Dashboard `period=YYYY-MM` search param has no far-future/far-past bound; the month stepper can walk arbitrarily far in either direction, always rendering an empty summary past the data range. Consistent with the existing transactions list month navigation — bound both together if desired.
