# Decision Log — supertool PRD

## 2026-06-09

- **Intent**: Create PRD for supertool platform + Money Tracker v1.
- **Primary input**: finalized product brief at `_bmad-output/planning-artifacts/briefs/brief-supertool-2026-06-09/` (brief.md + addendum.md + .decision-log.md) — produced earlier this session, fully in context.
- **Stakes (carried from brief)**: internal/pitch — dual purpose: real personal tool + BMad/Fable-5 methodology showcase for the Howly team. PRD rigor calibrated to internal-tool level (~5-8 pages) with pitch-grade traceability.
- **Form factor (carried)**: web app; pnpm + Turborepo monorepo; Next.js frontend + NestJS API.
- **Concern scan (initial)**: monorepo platform foundation (umbrella anticipating tool #2), tooling/CI migration constraints (oxlint/oxfmt, merged .coderabbit, merged AI setup), auth boundary (better-auth × NestJS), data seeding/import integrity (1,880-record JSON, money math), i18n from day one, test-per-feature quality bar, clean commit-history as success criterion.

### Discovery (user answers)

- **Usage pattern**: daily use; primary actions = create transactions, view transactions for a range (monthly default), check stats. Entry speed and monthly view are first-class requirements.
- **UX**: example app's UX/flows are satisfactory — mirror them; UI stays the same (carried design system). No new design references needed.
- **Platform semantics (user)**: tools are *separate apps* in the monorepo; shared shell AND shared packages; single login — same user account works across all apps. Auth/session must be shared across apps from day one.
- **Working mode**: Fast path (confirmed).

### Consolidated gap answers

- **Locales v1**: English + Ukrainian from day one (proves i18n machinery; matches real usage).
- **Currency aggregation**: currency filter (example-app pattern) — dashboard stats computed per selected currency; no cross-currency math in v1.
- **Seed data privacy**: keep repo private, commit the real seed file. User accepts real data in a private repo shared with trusted team viewers.
- **Timeline**: no hard deadline — artifact-trail quality over speed.

### Draft review (user)

- **Categories are hierarchical** (user correction): parent/child as in the example app — FR10 updated, FR11 seeds flat strings as top-level, FR12 covers child reassignment, FR15 breakdown grouped by top-level [ASSUMPTION].
- All other assumptions implicitly confirmed (only #5 corrected): no password recovery, unrestricted sign-up, profile settings w/ default currency, note field, delete-requires-reassignment, 12-month trend window, design-verified platform metric.
- `bmad-ux` marked likely-skippable stands (user did not object).

### Finalize — reconciliation & reviewer gate (2026-06-09)

- Input reconciliation (`reconcile-brief.md`) and rubric review (`review-rubric.md`) run as parallel subagents. Rubric verdict: **Pass** — 0 critical, 0 high, 2 medium, 5 low.
- **Settings contradiction (HIGH, reconcile)**: brief deferred "settings"; PRD FR5 ships a minimal profile subset. Resolution: FR5 explicitly supersedes the brief's blanket deferral; "full settings (password change, account deletion)" added to out-of-scope.
- **Stack not carried (reconcile + rubric)**: added ED4 locking the brief's stack in the PRD (self-sufficiency for architecture extraction).
- **FR4 acceptance vague (rubric medium)**: concrete acceptance added (hypothetical `apps/planner` consumes shell/auth/packages unmodified; nav via config only).
- **Assumptions index (rubric medium)**: "Assumptions status" section added; confirmed assumptions resolved into plain requirements, tags removed.
- **Mobile entry dropped (reconcile)**: NFR8 added — daily-entry flow usable in mobile browser, responsive, no native/PWA. [ASSUMPTION] pending operator confirmation.
- **Minor fixes**: build job added to process metric; rules + hooks added to ED3; FR6 dangling "confirm" resolved (note field confirmed earlier); EN+UK locales were a user decision (no tag needed); UX-approval provenance = user statement this session ("UX and flows currently satisfies me", "UI remaining the same").
- **Set aside (low, stakes-appropriate)**: glossary/term-drift, dashboard period granularity wording, FR16 trend-shape detail — owner: architecture/epics phases; revisit if extraction stumbles.
- **Open at finalize**: [ASSUMPTION] FR15 (breakdown grouped by top-level), [ASSUMPTION] NFR8 (mobile-usable) — non-blockers, owner: Oleksii, revisit on PRD review or first dashboard/UI story.
- **Open question carried**: better-auth × NestJS boundary + cross-app session — owner: architecture phase (blocks auth stories there, not the PRD).

### Closed (2026-06-09)

- Editorial polish applied to prd.md (NFR ordering, ED3 split to bullets, heading consistency) and addendum.md (two prose fixes) — no facts changed.
- `prd.md` status set to `final`. No external handoffs configured. Two non-blocking [ASSUMPTION] tags remain (FR15, NFR8), surfaced to operator at close. Next: `bmad-create-architecture`.
